# CLAUDE.md — MT6735M kernel-3.18 显示启动崩溃修复

## 工作约定（必须遵守）

1. **用户手动编译**：不要主动执行全量/内核编译（如 `./build.sh`、`make` 出镜像等）。只改源码与配置说明；编译、刷机由用户完成。验证以用户提供的 `putty.log` / 串口结果为准。
2. **禁止越界读树**：工作范围 **仅限本目录** 
   `/home/maozi/orange_6735_3.18.79` 
   **不要**查看、搜索、对比上级目录或其它项目（含 `../` 下的另一套 kernel、旧固件源码树等）。需要对照旧行为时，只根据用户描述与本树内串口日志推断。
3. **项目目的与差分前提**：
   - 目标：给 **一个使用旧lk分区** 的设备 **适配新版本kernel**重点在于显示的修补（本树补 LCM / display 相关 kernel 改动）
   - 用户替换范围 **只有 kernel 镜像**；**未改动** ramdisk、system、vendor 等。分析与修改应优先怀疑本 tree 内 display/LCM/与之耦合的初始化路径
4. **说中文**用户有ADHD，不要给出意义不明确或者模棱两可的答案，请根据事实给出结论
---

## 工程摘要

| 项 | 值 |
|----|-----|
| 平台 | MT6735M (DT) |
| Kernel | Linux 3.18.79 |
| 工作目录 | `/home/maozi/orange_6735_3.18.79`（**唯一允许访问的工程树**） |
| 串口日志 | `putty.log` |
| 编译 | **用户手动**；助手不代为编译 |

---

## 阶段问题（Problem Timeline）

kernel和lk传递的lcm名字不匹配导致直接panic
